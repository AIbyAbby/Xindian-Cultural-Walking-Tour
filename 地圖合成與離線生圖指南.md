# 🗺️ 碧潭/新店溪走讀系列地圖合成與離線地圖生圖指南

本指南用作使用者（你）與 AI（Antigravity/Codex）協作進行**離線地圖相片提取、優化與大圖合成**的工作手冊。當你需要製作新的走讀地圖（如走讀三、走讀一等）時，可以直接提供此指南，讓 AI 了解所有檔案路徑、執行步驟與版面規範。

---

## 📁 1. 統一資料夾與路徑規範

為確保腳本能正常執行，輸入與輸出檔案需統一存放於以下路徑（以 `C:\Users\abby8\Desktop` 為基礎）：

| 檔案/資料夾類別 | 預設路徑 | 說明 |
| :--- | :--- | :--- |
| **生圖來源資料夾** | `C:\Users\abby8\Desktop\新增資料夾` | 存放從離線地圖匯出的 `26-Icon.html` 網頁。 |
| **真實照片輸入** | `C:\Users\abby8\Desktop\走讀X` | 由 HTML 提取並經 ffmpeg 優化後的 26 張照片（例如：`走讀三`、`走讀四`）。 |
| **底圖輸入** | `C:\Users\abby8\Desktop\在溪與城之間_走讀X_導覽地圖.jpg` | 使用者標示好紅圈數字或位置的底圖。 |
| **合成腳本存放** | `C:\Users\abby8\Desktop\網站\scratch\` | 存放所有 PowerShell 處理腳本（如 `combine_map.ps1`）。 |
| **景點名稱清單** | `C:\Users\abby8\Desktop\網站\scratch\spots.txt` | 存放 26 個景點名稱的 UTF-8 文字檔（每行一個景點，不含編號）。 |
| **輸出完成資料夾** | `C:\Users\abby8\Desktop\完成` | 存放最終合成好的超高清大圖（如 `走讀地圖3_高解析修正版.png`）。 |
| **專案圖片目錄** | `C:\Users\abby8\Desktop\網站\images\` | 同步複製一份大圖與說明圖到網頁專案目錄下，供網頁載入。 |

---

## 📸 2. 離線相片提取與色彩優化流程

當你從離線地圖匯出 HTML 檔案時，相片通常為 WebP 格式且色彩較為黯淡。AI 會執行以下自動化流程：

1. **解析 HTML**：讀取 `C:\Users\abby8\Desktop\新增資料夾\26-Icon.html`，提取點 1 至 26 的 WebP 照片。
2. **色彩濾鏡優化**：使用系統內建的 `ffmpeg` 對每張照片進行對比度與飽和度提升，以符合水彩手繪風網頁的鮮豔調性。
   - **優化指令**：
     ```powershell
     ffmpeg -y -i <輸入.webp> -vf "eq=contrast=1.12:saturation=1.55" -update 1 <輸出.jpg>
     ```
3. **更名與存檔**：將優化後的照片，自動按照 `spots.txt` 順序更名為 `1_景點名稱.jpg` 至 `26_景點名稱.jpg`，並儲存至桌面 `走讀X` 資料夾。

---

## 🎨 3. 地圖大圖合成排版規範

目前地圖合成支援兩種主流排版風格，AI 會根據各走讀路線（如走讀二、三、四）的特性執行對應腳本：

### 🎴 風格 A：網格方形圖卡版（適用於 走讀二、走讀三）
* **設計特色**：26 張手繪插圖以方形圓角白底小圖卡（76x76 像素，帶金金色邊框）排列於地圖外圍網格中，左上角配有紅底白字圓圈編號，並以**紅色手寫風虛線**連結至地圖上的對應點。
* **對應腳本**：`C:\Users\abby8\Desktop\網站\scratch\generate_walking_map_3.ps1`
* **最終輸出**：桌面 `完成` 資料夾下的 `走讀地圖3_高解析修正版.png` 與 `走讀三_合成草稿.jpg`。

### 📑 風格 B：左側清單邊欄版（適用於 走讀四）
* **設計特色**：左側為 398 像素寬的奶油色清單邊欄，垂直排列 26 個景點名稱與紅色數字徽章，下方附有 QR Code 與「用腳步閱讀這座城市」字樣。右側為 1273 像素寬的無縫滿版地圖。
* **對應腳本**：`C:\Users\abby8\Desktop\網站\scratch\combine_map.ps1`
* **最終輸出**：桌面 `完成` 資料夾下的 `走讀地圖4_高解析修正版.png` 與 `在溪與城之間_走讀四_導覽地圖_合併版.jpg`。

---

## 💬 4. 溝通指令對話範本

當你準備好檔案並需要 AI 處理時，可以直接**複製並修改**以下指令發送給 AI：

### 📝 範本一：提取離線地圖照片並優化
> **使用者指令**：
> 「我已經把走讀三的離線地圖 `26-Icon.html` 放進桌面『新增資料夾』了。請幫我解析並提取照片，使用 ffmpeg 進行對比度 1.12、飽和度 1.55 的色彩優化，然後按照 `spots.txt` 的順序更名，存入桌面的『走讀三』資料夾中。」

### 🎨 範本二：執行地圖大圖合成 (走讀三 - 網格方形圖卡版)
> **使用者指令**：
> 「我已經把走讀三標示好的底圖放上來了，並且 spots.txt 與優化後的照片都已就緒。請幫我執行桌面 `網站\scratch\generate_walking_map_3.ps1` 進行地圖合成，並將產出的無損 PNG/JPG 存入桌面『完成』資料夾中，同時更新網站 images 目錄下的地圖檔。」

### 🎨 範本三：執行地圖大圖合成 (走讀四 - 左側清單邊欄版)
> **使用者指令**：
> 「我已經把走讀四標示好的底圖放上來了，請幫我執行桌面 `網站\scratch\combine_map.ps1` 進行左側邊欄清單的合成，並將產出的 JPG 存入桌面『完成』資料夾中。」
